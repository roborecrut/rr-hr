План исправления:

1. **Починить редактор компании**
   - Убрать ложное состояние «Документ загружен» до реальной загрузки в Supabase.
   - После выбора файла показывать статус «Загружаем в Supabase…».
   - Только после успешного `supabase.storage.from('company-uploads').upload(...)` сохранять `draftFilePath` и показывать кнопку **«Отправить документ в ProTalk»**.
   - Если нет Supabase-сессии или черновика компании — показывать понятную ошибку, а не делать вид, что файл загружен.
   - После ответа ProTalk заполнять поле текста, затем оставлять кнопку **«Оформить красиво с помощью ИИ»** для разнесения по полям.

2. **Сделать общий двухшаговый загрузчик для редакторов**
   - Обновить `DocumentIngestField`: файл сначала загружается в нужный bucket, затем появляется отдельная кнопка **«Отправить документ в ProTalk»**.
   - Применить это для редакторов, которые используют общий компонент: обучение, вакансии/интервью при подключении через `DocumentIngestField`.
   - Добавить явные состояния: загрузка в Supabase, файл загружен, отправка в ProTalk, ошибка загрузки.
   - Не удалять локальный input/название файла раньше времени; очищать только после успешного цикла или отмены.

3. **Починить скрининг кандидатов и аватарки**
   - Сейчас кандидаты авторизуются через собственную `candidate_sessions`, а не через Supabase Auth, поэтому прямой upload в `candidate-resumes` / `candidate-avatars` может блокироваться RLS.
   - Сделать безопасную Edge Function для загрузок кандидата: она проверит `candidate_sessions.token`, загрузит файл в правильный bucket через service role и вернёт путь/ссылку.
   - Перевести загрузку резюме в `CandidateInterview` и фото профиля в `CandidateFlow` на эту функцию.
   - Для резюме после успешной загрузки показывать кнопку **«Отправить резюме в ProTalk»**, затем заполнять текст резюме.

4. **Исправить доступ ProTalk к файлам**
   - В `ai-ingest-document` для private bucket `candidate-resumes` использовать signed URL вместо public URL, чтобы ProTalk реально мог скачать файл.
   - Для публичных buckets оставить текущую схему, но сделать её устойчивее: если bucket приватный — всегда signed URL.

5. **Проверить storage-политики и миграции**
   - Бакеты уже есть; дополнительно проверю/обновлю RLS только там, где нужно для загрузчиков работодателя.
   - Для кандидатских upload-flow не расширять публичный доступ: загрузка будет идти через проверенную Edge Function по токену кандидата.

6. **Визуально привести загрузчики к стилю бренда**
   - Везде убрать серые/белые состояния из загрузочных блоков.
   - Использовать Apple Liquid Glass на синем градиенте, белый текст, золотой акцент и брендовые кнопки, согласно сохранённой палитре.

Технически будут затронуты:
- `src/pages/EmployerPanel.tsx`
- `src/components/DocumentIngestField.tsx`
- `src/components/CandidateInterview.tsx`
- `src/pages/CandidateFlow.tsx`
- `supabase/functions/ai-ingest-document/index.ts`
- новая Edge Function для кандидатских загрузок
- при необходимости миграция только для storage/RLS