## План правок

### 1. CRM (Канбан + Таблица) — `EmployerPanel.tsx`
- Зафиксировать высоту области канбана: `h-[calc(100vh-260px)] overflow-auto`, горизонтальный скролл по контейнеру, вертикальный — внутри.
- Нижняя полоса прокрутки — sticky к низу области (а не страницы).
- Шапка меню при переходе в CRM «плывёт» — заменить `position:absolute`/transform на стабильный flex, не сбрасывать `overflow` на body.
- Таблица — обернуть в `overflow-auto` с такой же фиксированной высотой.

### 2. Карточка кандидата — `CandidateDetailsModal.tsx`
- Убрать дублирование «Этап воронки» из профиля (оставить только в шапке-статусе).
- Убрать все технические ID (`№ 200011`, `/300020`, `ID: 400020`, «Профессия»-badge рядом с именем).
- Переименовать тайл **Интервью → Средний балл** (а текущий «Средний» убрать как дубль, либо переименовать общий тайл).
- Добавить 5-ю вкладку **«Общая оценка»** — выводит финальный балл + рекомендацию ИИ (соответствие должности).
- Во вкладке «Ситуации»: вместо `s1/s2/s3` — текст кейса из `interview_blocks` (kind=situations), отдельно показывать ответ кандидата и оценку ИИ.
- В блоке «Решение по кандидату» добавить кнопку **«На рассмотрении»** (статус `review`, тег жёлтый).
- Резюме-оценка ИИ — обновить промт в `ai-interview-screen-resume` чтобы возвращал более развёрнутый экспертный анализ (сопоставление требований вакансии ↔ резюме, 6-10 предложений в `summary`).

### 3. Тариф & Счета — `EmployerPanel.tsx`
- Вкладка **Покупка и оплата**: блок «Пополнение баланса (Робокасса)» — на 1-е место.
- Кнопки «Приобрести +1» / «Купить пакет» → текст **«Списать за RR»**, обе зелёные (`bg-emerald-600`).
- Вкладка **Баланс**: лицевой счёт и купленные пакеты — на 1-е место (HiringCalculator вниз).
- Добавить 5-ю вкладку **«Тарифы и цены»**, перенести туда блок тарифов из «Баланс».
- Внутри «Тарифы и цены» — разделить на 2 подблока: «Разовые услуги при создании вакансии» и «Стоимость интервью/обучения», сетка `grid md:grid-cols-2 gap-4`.

### 4. Мои Компании — `EmployerPanel.tsx`
- Карточку зарегистрированной компании выделить рамкой/фоном (border + bg-card), отделить от соседних блоков.

### 5. Починка ИИ в мастере вакансий
- Проверить `ai-enhance`/`VacancyAIAssistant`: судя по жалобе, любой клик → ошибка. Скорее всего — упавший edge function или `friendly()` глотает реальную причину.
- Прологировать `error.context.json()` целиком в `aiClient.ts` (console.error для отладки), проверить логи edge function, починить.

### Файлы
- `src/pages/EmployerPanel.tsx`
- `src/components/CandidateDetailsModal.tsx`
- `src/components/VacancyAIAssistant.tsx` / `src/lib/aiClient.ts`
- `supabase/functions/ai-interview-screen-resume/index.ts` (промт)
- возможно `supabase/functions/ai-enhance/index.ts` (диагностика)

Запускать?
