## Итерация 1 — Критические фиксы AI и логики (СЕЙЧАС)

Это блокирует пользователей. Делаю в первую очередь.

1. **AI-консультант ProTalk на лендинге вакансии/компании** — починить «Сетевая ошибка», передавать данные вакансии и вопрос пользователя в модель 66337.
2. **`/restart` на странице `/interview/resume`** — реально отправлять команду, ждать ответ от нейросети (как в остальных перезапусках), и только потом открывать страницу.
3. **Чеклист — парсинг JSON ответа** — нейросеть возвращает корректный JSON в \`\`\`json блоке, но мы парсим неверно. Исправить экстракцию JSON (вытащить из markdown-блока), правильно проставить score каждого текстового вопроса, итоговый total (80/100), выводить в UI поля `explanation`, `what_was_right`, `what_was_wrong`, `strengths`, `gaps`, `summary`.
4. **Тесты обучения (`/training/professional`, `/training/product`, `/training/system`)** — отправлять все 20 вопросов с эталонами одним батчем (как чеклист), получать JSON, парсить, писать в БД, показывать оверлей ожидания, обновлять UI без перезагрузки страницы, ставить `passed_at` в `candidate_stage_progress` при ≥ pass_score.
5. **`ai-generate-training-quiz`** — нейросеть возвращает поле `correct` строкой (название правильного варианта) вместо флага `is_correct` внутри options — иногда. Сделать парсер устойчивым: если есть `correct`, проставить `is_correct` в options автоматически. Если парсинг провалился, показать в оверлее кнопку **«Сгенерировать через RR Pro Max»** (она уже существует у нас в системе для fallback).
6. **`/scoring`** — добавить блок итоговой оценки чеклиста (score, max, summary, strengths, gaps) рядом с оценкой резюме и ситуаций.

## Итерация 2 — Ребрендинг и анимации

Голубой градиент `#17344F → #265582`, золотой `#F4EE8E → #D99E41`, синий `#1E4468` (текст кнопок), золотой `#E7C768` (бордюры), белый (основной текст).

- Лендинг вакансии: 8 подстраниц (`/vacancy`, `/schedule`, `/motivation`, `/payouts`, `/onboarding`, `/team`, `/system`).
- ЛК кандидата: все страницы из списка пользователя (`/interview/resume`, `/profile`, `/scoring`, `/terms/*`, `/training/*`, `/certified`).
- Заменить все тёмные/чёрные тона на голубой градиент.
- Анимировать блоки при скролле — плавное появление с разных сторон (использовать существующий `Reveal.tsx`).

## Итерация 3 — Оптимизация и чистка

- **Логотип компании** — universal optimizer: при загрузке файла в storage ресайз до **256×256 WebP** (canvas → blob), для внешних URL — использовать как есть с lazy + правильными размерами. Проверить везде: header, hero вакансии/компании, карточка вакансии/компании, AI-консультант (иконка + шапка диалога).
- Удалить из `/profile` кандидата разделы «Файловое досье кандидата» и «📎 Дополнительные документы».

## Техническая часть

**Файлы итерации 1:**
- `supabase/functions/ai-interview-grade-checklist/index.ts` — устойчивый JSON parser (regex extract из ```json блока), пробросить новые поля
- `supabase/functions/ai-generate-training-quiz/index.ts` — нормализация `correct` → `is_correct`
- Новый edge function `ai-grade-training-quiz` — батч-оценка всех ответов теста за один запрос (по модели `ai-interview-grade-checklist`)
- `src/components/CandidateStageTraining.tsx` — переключить с поштучной проверки `ai-check-stage-answers` на батч, оверлей ожидания, авто-обновление progress
- `src/components/CandidateInterview.tsx` или соответствующий resume-компонент — добавить ожидание /restart
- `src/components/CompanyLanding.tsx` / `VacancyAIAssistant.tsx` — починить вызов ProTalk, передавать контекст вакансии
- `src/pages/CandidateFlow.tsx` (раздел scoring) — добавить блок итога чеклиста
- Миграция: добавить колонки `summary`, `strengths`, `gaps` в `candidate_scores` если их нет (для чеклиста)
- Парсер в `EmployerPanel.tsx` или соответствующем месте — обработать оверлей ошибки + кнопка RR Pro Max

**Подтверждение:** начинаю с Итерации 1. Когда закончу — проверю в браузере, потом перехожу к 2 и 3. Подтвердите, и я начну.
